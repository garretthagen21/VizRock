# Requires Python >= 3.10 (brain.py uses PEP 604 `X | None` annotations).
# Raspberry Pi OS Bookworm ships 3.11.
mido==1.3.2
python-rtmidi==1.5.8
pyserial==3.5
aiohttp==3.9.5
# OLED (optional — only needed on the Pi with a panel wired up)
luma.oled==3.13.0
Pillow>=10.0
